import React from "react";
import { Feather } from "@expo/vector-icons";

const SearchButton = () => {
  return (
      <Feather name="search" size={24} color={"grey"} style={{ marginLeft: 8 }}/>
  );
};

export default SearchButton;
